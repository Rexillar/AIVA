# ✅ AIVA Setup Checklist

Here is what you need to do to get AIVA running on your machine.

### 1. Prerequisites
- [ ] Install **Docker Desktop** (and make sure it's running).
- [ ] Create a folder for this project (e.g., `my-aiva`).
### 3. Run Application
- [ ] Open a terminal (CMD or PowerShell) in your folder.
- [ ] Run this command:
  ```bash
  docker compose up -d
  ```
- [ ] Wait for the images to download (this might take a few minutes).



### 4. Verify & Use
- [ ] Open your browser to: **http://localhost:3000**
- [ ] Register a new account or Login.
- [ ] Enjoy! 🚀

--
**Troubleshooting:**
- If you see "User not found" on login, ensure you are using the correct Email.
- If the page doesn't load, check if Docker is running (`docker ps`).
